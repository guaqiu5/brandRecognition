
import './App.css'
import {useLayoutEffect} from 'react'
function App() {

  // useLayoutEffect(()=>{
  //   import('./utils/index')
  // })

  return (
    <div className="App">
   {/* <input type="file" onChange={(e)=>{
     console.log(typeof window.predict,`fun`)
     window.predict(e.target.files[0])
   }}/>
    <button onClick="download()">下载模型</button>   */}
    </div>
  )
}

export default App
